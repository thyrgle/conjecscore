<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Conway's 99 Conjecture</title>
  <link href="{{ url_for('static', path='/output.css') }}" rel="stylesheet">
  <link rel="shortcut icon"
	href="{{ url_for('static', path='/butterfly.svg') }}"
	type="image/svg" />
  <link rel="shortcut icon"
        media="(prefers-color-scheme: light)"
	href="{{ url_for('static', path='/butterfly.svg') }}"
	type="image/svg" />
  <link rel="shortcut icon"
        media="(prefers-color-scheme: dark)"
	href="{{ url_for('static', path='/dark-mode-butterfly.svg') }}"
	type="image/svg" />
  <script src="{{ url_for('static', path='/login.js') }}"></script>
</head>
<body class="dark:bg-black dark:text-white">
    <form id="form"
	  class="max-w-md mx-auto mt-8 dark:bg-gray-900 bg-white p-6 rounded shadow">
      <div class="pb-8"><h1 class="text-center">Log in</h1></div>
      <div class="mb-4 flex items-center">
        <label for="username"
	       class="w-32 dark:text-gray-200 text-gray-700 font-semibold">
	  Email:
	</label>
        <input type="email"
	       id="email"
	       name="email"
	       class="flex-1 border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring focus:border-blue-400"
	       required />
        </div>
        <div class="mb-4 flex items-center">
	  <label for="password" 
		 class="w-32 dark:text-gray-200 text-gray-700 font-semibold">
	    Password:
	  </label>
	  <input type="password"
	         id="password"
		 name="password"
		 class="flex-1 border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring focus:border-blue-400"
		 required />
        </div>
        <div class="flex justify-end">
	  <input type="submit"
		 id="submit"
		 value="Submit"
		 class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">
	  </input>
        </div>
    </form>
    <div id="loginstatus">
      <!-- Prints whether person successfully logged in or not. -->
    </div>
</body>
</html>
